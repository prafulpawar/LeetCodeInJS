/**
 * @param {string[]} arr
 * @param {number} k
 * @return {string}
 */
var kthDistinct = function(arr, k) {
    
};

let arr = ["d","b","c","b","c","a"], k = 2
let x = kthDistinct(arr,k);
console.log(x);