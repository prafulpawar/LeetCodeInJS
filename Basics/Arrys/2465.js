/**
 * @param {number[]} nums
 * @return {number}
 */
var distinctAverages = function(nums) {
    // let i = nums.length
    // while(nums[i]!= 0)
};

let nums = [1,100]
let x = distinctAverages(nums)
console.log(x);
