/**
 * @param {number} money
 * @param {number} children
 * @return {number}
 */
var distMoney = function(money, children) {
    
};

let money = 20
let children = 3

console.log(distMoney(money, children)) 