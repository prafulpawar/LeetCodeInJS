/**
 * @param {number[]} nums
 * @param {number[]} divisors
 * @return {number}
 */
var maxDivScore = function(nums, divisors) {   
      for(let i =0;i<nums.length;i++){

      }
};

let nums = [2,9,15,50]
let divisors = [5,3,7,2]
let x = maxDivScore(nums, divisors)
console.log(x)